package com.wzw.algorithm.slidingwindow;

import com.wzw.algorithm.dichotomy.DichotomyUtil;

/**
 * @author wuzhiwei
 * @team M
 * @owner wuzhiwei
 * @Date 2022/2/10
 */
public class SlidingWindowUtil {


    /**
     * 给定一个含有 n 个正整数的数组和一个正整数 target
     * 找出该数组中满足其和 ≥ target 的长度最小的 连续子数组 [numsl, numsl+1, ..., numsr-1, numsr] ，并返回其长度。如果不存在符合条件的子数组，返回 0
     * 例如：<br>
     * 输入：target = 7, nums = [2,3,1,2,4,3] <br>
     * 输出：2 <br>
     * 解释：子数组 [4,3] 是该条件下的长度最小的子数组。<br>
     * @see DichotomyUtil#minSubArrayLen(int, int[])
     */
    public static int minSubArrayLen(int target, int[] nums) {
        //
        return 0;
    }
}
